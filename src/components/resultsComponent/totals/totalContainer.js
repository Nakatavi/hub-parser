import React from 'react';
import TotalHours from './totalHours';

const TotalContainer = (data, sets) => {
    return (
        <div className='total-container'>
            hello
            <TotalHours/>
        </div>
    );
}

export default TotalContainer;
